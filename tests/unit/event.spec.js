const event = require("../../src/event/event.controller");
const request = require('supertest');

describe("Get Event using Promise", () => {
    it("load data", ()=> {
        
        expect(1).toBe(3);
    }); 
});

